import 'package:flutter/material.dart';

class ItemcomentariosEmergencia extends StatelessWidget {
  const ItemcomentariosEmergencia({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
